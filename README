Le reload redemarre les programme meme quand rien ne change ==> OK

La sortie d'erreur de tail -f (quand le fichier n'existe pas) s'affiche apres le prompt ==> OK

Les programme ne se redémarre pas lorsque l'on le kill ==> OK

Pourquoi il n'y a rien dans /tmp/taskmaster.log // lorsqun procecuss fils essaye decrire dans le taskmaster celui ci n'est pas ouvert

Fair une map pour tous le signaux (feature)

Checker le resultat lorsque le dir nest pas autoriser ou lorsque le fichier de log nest pas valide

Regarder pk le prompt ne saffiche pas tjs quand il faut